import styled from 'styled-components';
import { theme } from '../../styles';

export const Title = styled.h1`
  font-size: ${theme.typography.fontSize.fb};
  color: ${theme.colors.title};
  font-weight: ${theme.typography.fontWeight.semiBold};
  margin: 0;
  padding: 0;
`;
